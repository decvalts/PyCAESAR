language: python
python:
  - "2.7"

install: "pip install -r requirements.txt"

# here you would call the testing script
# but I need to check this first...
# come back soon!
script:
  python tests/test_1.py
